use_default_settings: true
server:
  secret_key: "ricette-locali-development-key"
  bind_address: "0.0.0.0"
  port: 8080
search:
  formats:
    - html
    - json
ui:
  static_use_hash: true

engines:
  - name: duckduckgo
    engine: duckduckgo
    shortcut: ddg
    disabled: false
